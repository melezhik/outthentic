language: "perl"
perl:
  - "5.20"

script: "cpanm --installdeps . && perl Makefile.PL && make test && make install && find ./examples -name story.rb -or -name story.bash -or -name story.pl -execdir pwd \\; "
